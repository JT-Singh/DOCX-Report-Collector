# DOCX-Report-Collector
This tool recursively scans a directory structure looking for DOCX files whose filenames end with "_FINAL.docx". All matching files are copied into a folder named "FINAL_TRs" created in the directory where the script is executed.
